# frozen_string_literal: true

source "https://rubygems.org"

# The SDK itself has zero runtime deps (pure stdlib). This Gemfile only sources the gemspec so
# `bundle exec rake test` works; minitest and rake ship with Ruby as default gems.
gemspec
